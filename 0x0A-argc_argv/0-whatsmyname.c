#include <stdio.h>
/**
 * main - prints the name if program
 *
 * @argc: no of cmd line arg
 * @argv: array of cmd line arg
 * Return: 0
 */
int main(int argc __attribute__ ((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
