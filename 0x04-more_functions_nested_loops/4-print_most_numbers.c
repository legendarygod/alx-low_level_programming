#include "main.h"

/**
 * print_most_numbers - prints 0-0 grep 2, 3
 * Description: prints
 * Return: void
 */

void print_most_numbers(void)
{
	int a = 0;

	while (a <= 9)
	{
		if (a != 2 && a != 4_)
		{
			putchar(a + '0');
		}
		a++
	}
	_putchar('\n');
}
